/**
 * In these examples, the notion of batch sanitization is
 * introduced and shown. During batch sanitization, duplicate
 * and possibly redundant updates are removed.
 */
package dna.examples.batches.sanitization;

